[package]
name = "zpl_toolchain_core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core parser, emitter, and validator for ZPL II label code"
keywords = ["zpl", "zebra", "label", "parser"]
categories = ["parsing", "text-processing"]

[lib]
name = "zpl_toolchain_core"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
zpl_toolchain_diagnostics = { path = "../diagnostics", version = "0.1.0" }
zpl_toolchain_spec_tables = { path = "../spec-tables", version = "0.1.0" }
zpl_toolchain_profile = { path = "../profile", version = "0.1.0" }
